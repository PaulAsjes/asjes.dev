---
title: "Syntax Highlighting Test"
date: 2024-02-01
description: "A test post exercising code block features: language detection, line highlighting, titles, and inline code."
tags: [meta, javascript]
---

This post exists to verify syntax highlighting works correctly.

## Basic block

```javascript
function greet(name) {
  return `Hello, ${name}!`;
}

console.log(greet("world"));
```

## TypeScript

```typescript
type User = {
  id: string;
  name: string;
  role: "admin" | "user";
};

async function fetchUser(id: string): Promise<User> {
  const res = await fetch(`/api/users/${id}`);
  if (!res.ok) throw new Error("Not found");
  return res.json();
}
```

## Line highlighting

Lines 2 and 4 are highlighted below:

```javascript {2,4}
function add(a, b) {
  // this line is highlighted
  const result = a + b;
  return result; // this one too
}
```

## With filename

```typescript title="lib/blog.ts"
import fs from "fs";
import path from "path";

export function getPost(slug: string) {
  const filePath = path.join(process.cwd(), "content/blog", `${slug}.mdx`);
  return fs.readFileSync(filePath, "utf-8");
}
```

## Line numbers

```javascript showLineNumbers
const items = [1, 2, 3, 4, 5];

const doubled = items
  .filter((n) => n % 2 === 0)
  .map((n) => n * 2);

console.log(doubled); // [4, 8]
```

## Inline code

Use `console.log()` for debugging. The `typeof` operator returns a string like `"string"` or `"number"`.

## Shell

```bash
pnpm install
pnpm dev
GH_TOKEN=ghp_xxx pnpm migrate
```
